#include "scheduler.h"
#include <kernel/interfaces/interrupts.h>

#include <stdint.h>

typedef struct {
	arch_register_set_t register_set;
} kernel_thread_t;

typedef struct {
	
} kernel_process_t;

int kernel_schedule_threads() {
	
}

kernel_thread_t* kernel_scheduled_thread() {
	
}

int kernel_scheduler_init() {
	
}
